declare namespace App {
  import type { Theme } from 'config/theme';
  // interface Stuff {}
  interface Locals {
    theme: Theme | null;
  }
  // interface Platform {}
  // interface PrivateEnv {}
  // interface PublicEnv {}
}
